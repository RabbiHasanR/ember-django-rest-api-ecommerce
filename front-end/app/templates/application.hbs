<NavBar @logout={{action 'logout'}}/>
{{#each flashMessages.queue as |flash|}}
 {{!-- {{#flash-message flash=flash as |component flash|}}
    <h6>{{component.flashType}}</h6>
    <p>{{flash.message}}</p>
  {{/flash-message}} --}}
  {{!-- {{flash-message flash=flash}} --}}
  <FlashShow @flash={{flash}} />
{{/each}}


{{outlet}}
